# domain/

Pure, framework-free business logic — zero React/DB imports, 100%
unit-testable.

Every file below currently holds signature-only stubs — every function
throws `TODO: ...` and needs a real implementation (+ passing unit test,
for the money/stock-affecting ones) before its day is done:
- `fefo.ts`, `cashFormula.ts`, `discounts.ts` — Sales (Day 6-7)
- `credit.ts` — Customer/Credit (Day 9)
- `permissions.ts` — Staff (Day 11)
- `purchases.ts` — Supplier/Purchase (P1, post-beta)
