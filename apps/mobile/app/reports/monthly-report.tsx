import { Text, View } from 'react-native';

// Monthly Report — Volume 4 REPORTS. Explicitly P1 (Volume 0's scope lock:
// "Reports polish (Monthly Report, Data Export, Printer Settings)").
// TODO(P1): monthly aggregation via db/reports.ts's getMonthlyReport.
export default function MonthlyReportScreen() {
  return (
    <View>
      <Text>TODO: Monthly Report (P1, Volume 4 REPORTS)</Text>
    </View>
  );
}
