import { Text, View } from 'react-native';

// Sales History — Volume 4 REPORTS. Not in Volume 0's explicit P0
// day-by-day list; grouped with Volume 0's P1 "Reports polish" item.
// TODO(P1): paginated/filterable sales list via db/reports.ts's
//   getSalesHistory — read-only, local SQLite only.
export default function SalesHistoryScreen() {
  return (
    <View>
      <Text>TODO: Sales History (P1, Volume 4 REPORTS)</Text>
    </View>
  );
}
