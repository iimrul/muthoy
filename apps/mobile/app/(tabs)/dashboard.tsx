import { Text, View } from 'react-native';

// MorningDashboard — Volume 0 Day 5. Shell showing shop name + a greeting;
// deeper dashboard content (today's summary, etc.) isn't detailed beyond
// this in Volume 4, so kept minimal here rather than invented.
// TODO(Day 5): show shop name (from session) + a greeting.
// TODO(Day 5): no StandardHeader on this screen (Volume 4 NAVIGATION: header
//   applies to every screen except MorningDashboard and Registration).
// TODO(Day 5): this is the (tabs) group's landing tab.
export default function MorningDashboardScreen() {
  return (
    <View>
      <Text>TODO: MorningDashboard — shop name + greeting shell (Volume 0 Day 5)</Text>
    </View>
  );
}
