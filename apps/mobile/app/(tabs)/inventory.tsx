import { Text, View } from 'react-native';

// Inventory (list) — Volume 4 INVENTORY, Volume 0 Day 8.
// TODO(Day 8): list medicines — name, current total stock, batch count,
//   active-batch expiry countdown (db/inventory.ts's listMedicines, which
//   resolves the active batch via domain/fefo.activeBatch).
// TODO(Day 8): entry point to app/inventory/add-medicine.tsx.
export default function InventoryScreen() {
  return (
    <View>
      <Text>TODO: Inventory list — stock, batch count, expiry countdown (Volume 0 Day 8)</Text>
    </View>
  );
}
