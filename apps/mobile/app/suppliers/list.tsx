import { Text, View } from 'react-native';

// Supplier list — Volume 4 SUPPLIER/PURCHASE. P1 (post-beta fast-follow,
// Volume 0's scope lock) — Beta ships manual batch entry via Add Medicine
// instead (app/inventory/add-medicine.tsx). Do not implement before Day 15.
// TODO(P1): list suppliers (name/phone/address/email/contact_person) via
//   db/suppliers.ts's listSuppliers, with outstanding payable per supplier.
export default function SupplierListScreen() {
  return (
    <View>
      <Text>TODO: Supplier list (P1 — post-beta, Volume 4 SUPPLIER)</Text>
    </View>
  );
}
