# components/ui/

Generic, screen-agnostic building blocks (Header, PinPad, PlanBadge, buttons).

Currently signature-only stubs (every component renders a TODO placeholder):
`PinPad.tsx` (Day 4), `StandardHeader.tsx` (Day 5), `PlanBadge.tsx` +
`PremiumGate.tsx` (P1, post-beta — Subscription).
