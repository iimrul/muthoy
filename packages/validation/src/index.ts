// Empty scaffold — every Zod schema in the product lives here, starting Day 4.
// A form in apps/mobile and any admin-side write both import from here; a Zod
// schema is never redefined ad hoc inside a screen (DEVELOPMENT_RULES.md).
export {};
