import tokens from './tokens/fonts.json';

// CLAUDE.md rule 6: money uses fonts.dmMono, every other number/UI text uses
// fonts.plusJakartaSans, Bangla copy uses fonts.hindSiliguri. Never hardcode
// a font string directly in a component.
export const fonts = tokens;
